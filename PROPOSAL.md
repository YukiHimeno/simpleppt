# SimplePPT 功能补充提案

读完代码后整理的三个功能补充。都是围绕同一件事：**项目做完之后，怎么带走、怎么讲**。

## 现状

SimplePPT 的六阶段流水线（需求访谈 → 便利贴大纲 → 资料检索 → 页面策划 → 整页 SVG）已经完整，生成能力全部在浏览器本地执行。与「做完之后」相关的现状：

- 项目数据只存在 `localStorage`（键 `simpleppt.project.v1`），清缓存或换浏览器就没了，没有备份/迁移手段。
- 讲者备注（`PagePlan.speakerNote`）已经生成、写入 PPTX，但除了在页面下方显示一行小字，没有别的用法。
- 放映模式（`SlideStage` 内建的全屏黑色覆盖层）支持 ←→ 翻页、Esc 退出，但看不到讲者备注，也没有计时。

## 补充原则

- 继续保持「本地优先」：不加后端、不加新依赖，导出都是纯前端拼字符串 / 下载 Blob。
- 复用六阶段已有产物（outline / plan / speakerNote），不新增任何 AI 调用。
- UI 沿用 `ui.tsx` 现有组件与底栏菜单结构，不引入新交互范式。

---

## 提案一：项目备份（导出 / 导入 JSON）

**动机**：项目包含访谈回答、大纲、逐页检索资料、策划稿和幻灯片，是用户花了不少交互换来的东西，现在却锁死在单台浏览器里。换设备、清缓存、多设备接力，都做不到。

**方案**：

- 新增 `src/lib/project-io.ts`：
  - `exportProjectFile(project)`：把 `{ app: 'simpleppt', version: 1, exportedAt, project }` 序列化成 JSON 下载，文件名 `simpleppt-<主题>-<日期>.json`。
  - `readProjectFile(file)`：读文件、`JSON.parse`、校验 `topic` 字段存在，缺的字段用 `emptyProject()` 兜底，恢复到 `store.ts` 的导入路径。
- `src/lib/store.ts` 抽出 `resetTransientState(p)`：把 running 状态重置为 pending、清空 planStatus，`loadProject` 与导入共用（刷新后的行为和导入后保持一致）。
- `App.tsx` 底栏「设置」菜单加一组「项目」：导出项目备份 / 导入项目备份；导入先弹确认对话框（会覆盖当前项目，与「新建项目」对话框同款），解析失败弹错误对话框。

**验收**：演示模式走完流程 → 导出 JSON → 新建项目 → 导入 JSON → 六个阶段的产物完整回来，running 状态不残留。

## 提案二：讲稿导出（Markdown）

**动机**：排练演讲时打开 PPT 看备注不方便，一份纯文本讲稿（每页标题、一句话信息、讲者备注）更实用，也方便打印或贴进其他工具。

**方案**：

- 新增 `src/lib/notes-export.ts`：`buildSpeechMarkdown(project)` 按大纲顺序生成 Markdown——
  - 头部：主题、核心结论、需求摘要（受众 / 目的 / 场合 / 范围 / 基调 / 关键信息）。
  - 每页：`## 01 · 标题`、章节与页型、一句话信息、bullets 类卡片的要点、讲者备注（引用块）。
  - 尾部：导出时间与 SimplePPT 署名。
- `StageSlides` 工具栏加「导出讲稿」按钮，`downloadText(md, '<主题>-讲稿.md', 'text/markdown')` 下载。

**验收**：演示模式生成 8 页后导出讲稿，Markdown 结构完整、备注齐全，可直接预览。

## 提案三：演示者视图（放映时备注 + 计时）

**动机**：放映是最需要备注的场景，现在恰好看不到。演讲计时也是刚需（比如按 10 分钟排练），加一个毫秒成本的秒表就够。

**方案**（改 `SlideStage.tsx` 的放映覆盖层，保持黑底风格）：

- 放映时右上角显示计时（mm:ss，从进入放映起累计）。
- 工具行加「讲者备注」开关按钮，快捷键 `N`；展开后在幻灯片下方显示本页备注（无备注给提示文案），面板限高、可滚动。
- 翻页键补齐：空格 / PageDown 下一页，PageUp 上一页，Home / End 首尾页。

**验收**：放映中 N 呼出/收起备注，计时正常走，空格与方向键都能翻页，Esc 退出。

---

## 暂不做的想法

- **PDF 导出**：免依赖的做法只有调起打印对话框，体验绕；带依赖要引入 jsPDF，违背「不加新依赖」，先不做。
- **多项目管理**：需要把项目存储从单键改成列表并加管理 UI，动静大，值得单独一个提案。
- **自定义 Logo**：SVG 白名单里 `<image>` 是被禁的（校验器会拦截），要给风格系统开例外，涉及校验规则与导出两条链路，另行评估。
- **IndexedDB**：解决的是大文件参考资料的存储上限问题，当前 6 万字截断下 localStorage 尚未触顶。
